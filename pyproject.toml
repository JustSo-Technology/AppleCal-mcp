[project]
name = "macoscal-mcp"
version = "0.1.0"
description = "MCP Server"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.12.0",
    "uvicorn>=0.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
